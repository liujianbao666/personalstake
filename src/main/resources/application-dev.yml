#服务配置
server:
  port: 8081
  servlet:
    context-path: /

#字符编码
spring:
  http:
    encoding:
      charset: utf-8
      enabled: true
      force: true
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/yxcd?useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
#mybatis
mybatis:
  mapper-locations: classpath:com/kd/personalstake/mapper/*.xml